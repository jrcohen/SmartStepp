#include "TableData.h"


const float table1[] = {
  //Put encoder reference table here
  //Table for 1st motor instance
};

const float table2[] = {
  //Put encoder reference table here
  //Table for 2nd motor instance
};

const float table3[] = {
  //Put encoder reference table here
  //Table for 3rd motor instance
};

// Add additional tables for each extra motor controlled by this arduino board

